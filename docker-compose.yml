version: '3.8'
services:
  smart-money-bot:
    build: .
    container_name: smart-money-bot
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./reports:/app/reports
